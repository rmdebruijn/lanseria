# WireGuard config for NAS (Docker-based, linuxserver/wireguard)
# Deploy as Docker container — no DSM kernel modification needed
#
# docker run -d \
#   --name=wireguard \
#   --cap-add=NET_ADMIN \
#   --cap-add=SYS_MODULE \
#   -e PUID=1000 -e PGID=1000 \
#   -v /volume1/docker/wireguard/config:/config \
#   -p 51820:51820/udp \
#   --sysctl="net.ipv4.conf.all.src_valid_mark=1" \
#   --restart unless-stopped \
#   linuxserver/wireguard

[Interface]
Address = 10.100.0.2/24
PrivateKey = <NAS_PRIVATE_KEY>

[Peer]
# Vultr VPS
PublicKey = <VPS_PUBLIC_KEY>
Endpoint = 45.32.234.82:51820
AllowedIPs = 10.100.0.1/32
PersistentKeepalive = 25
